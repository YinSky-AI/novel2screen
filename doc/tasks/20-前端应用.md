# 模块 20：前端应用

> 单页 Web UI，支持小说上传、模式切换、结果预览与导出。

## 子任务

- [x] `index.html` — 三步骤流程（小说信息 → AI 分析 → 生成剧本）
- [x] `index.html` — 上传区域、模式切换、预览/导出区域
- [x] `app.js` — 模式切换、文件上传、进度条
- [x] `app.js` — 转换调用、结果显示、导出（下载 YAML / 复制）
- [x] `api.js` — `convertNovelText()` / `uploadNovelFile()` / `validateYaml()`
- [x] `main.css` — 深色主题、响应式设计、CSS 变量
- [ ] 实现 `uploadNovelFile()` 在 UI 中的调用（函数已定义但未使用）
- [ ] 实现 `/novels/upload` + `/generate/{task_id}` 的高级上传工作流 UI
- [ ] 实现 YAML 编辑器与编辑导入 UI（`/import-edits`）
- [ ] 实现一致性/对齐报告可视化（`/alignment`）
- [ ] 添加错误状态处理（当前错误显示原始 YAML）
- [ ] 添加语言选择（中文/英文）
- [ ] 添加流式响应进度显示
