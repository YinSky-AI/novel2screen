# 模块 01：基础架构与配置

> 负责系统配置管理、数据库 ORM 基础、LLM 多提供商客户端统一封装。

## 子任务

- [x] `config.py` — 集中管理环境变量（API 密钥、数据库 URL、Qdrant URL、MODE_DEFAULT 等）
- [x] `llm.py` — LLMClient 统一封装，按序尝试 DeepSeek → OpenAI → Anthropic → Ollama → 演示模式后备
- [x] `llm.py` — 演示模式（demo mode），无 API 密钥时返回固定剧本
- [x] `llm.py` — 用量统计追踪（`get_usage_report()`）
- [x] `database.py` — SQLAlchemy ORM 模型（Novel, Chapter, CharacterDB, EpisodeDB, SceneDB, BeatDB, HumanEdit, Export）
- [x] `database.py` — SQLite 数据库初始化与 Session 管理
- [ ] `database.py` — 迁移至 PostgreSQL 适配（可选）
- [ ] 合并 `novel2screen/backend/config.py` 中的额外配置项（MAX_INPUT_CHARS, DEEPSEEK_MAX_TOKENS 等）
- [ ] API 密钥安全管理，避免硬编码在 `.env` 中泄露
