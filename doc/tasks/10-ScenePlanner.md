# 模块 10：ScenePlanner（场景规划）

> 将每集分解为场景合约，定义地点、时间、目标、冲突和情感基调。

## 子任务

- [x] `scene_planner.py` — AgentBase 继承，`run()` 方法实现
- [x] `scene_planner.py` — `validate()` 方法（scenes ≥ 1）
- [x] 场景合约：`{"scene_id", "location", "time", "objective", "conflict", "emotion"}`
- [x] 约束：每集 2~12 个场景，按时间顺序排列
- [x] 场景 ID 格式：`sc_001`
