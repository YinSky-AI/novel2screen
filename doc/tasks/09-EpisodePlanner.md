# 模块 09：EpisodePlanner（剧集规划）

> 将小说结构映射为剧集结构，决定每集的标题、摘要和范围。

## 子任务

- [x] `episode_planner.py` — AgentBase 继承，`run()` 方法实现
- [x] `episode_planner.py` — `validate()` 方法（episodes ≥ 1）
- [x] 输入：叙事数据 + 角色数据 + 模式
- [x] 输出：`{"episodes": [{"id", "title", "summary", ...}]}`
- [x] 剧集 ID 格式：`ep_001`
