# 模块 21：人机协作工作流

> 用户编辑 YAML 后的导入、校验、差量评审与修复流程。

## 子任务

- [x] `POST /import-edits/{task_id}` — 上传编辑 YAML
- [x] `GET /alignment/{task_id}` — 一致性报告
- [x] `database.py` — HumanEdit / Export ORM 模型
- [x] 后端处理流程：
  - [x] 验证 YAML 格式（返回 422）
  - [x] 计算原始 vs 编辑的差异
  - [x] 仅对变更场景运行 CriticAgent
  - [x] 增量模式运行 RepairAgent
  - [ ] 可选运行 BidirectionalConsistencyAgent
- [x] Overlay 规则：
  - [x] 新增场景：直接接受
  - [x] 删除场景：从输出移除
  - [x] 修改 character_id：记录警告但保留
  - [x] 修改 visual_focus / sound_effect：始终接受
- [ ] 前端的 YAML 编辑器与导入 UI
- [ ] 差异对比可视化
