# 模块 17：记忆系统

> 短期记忆、长期记忆（角色圣经/世界观圣经）、语义记忆（ChromaDB RAG）。

## 子任务

- [x] `memory.py` — ShortTermMemory：对话缓冲（5 轮）、活跃章节/场景
- [x] `memory.py` — CharacterBible：持久化角色存储
- [x] `memory.py` — WorldBible：世界观规则 + 地理
- [x] `memory.py` — SemanticMemory：ChromaDB 向量存储，sentence-transformers 嵌入
- [x] `memory.py` — SemanticMemory：`chunk_text()` / `index()` / `search()` / `retrieve_context()`
- [x] `memory.py` — SemanticMemory：`_keyword_search()` Jaccard 关键词后备
- [x] `memory.py` — MemoryManager：协调所有记忆类型
- [x] `memory.py` — MemoryManager：`get_context()` / `detect_changes()` / `get_alignment_report()`
- [x] `memory.py` — 持久化存储 / 加载
- [x] `fact_extractor.py` — RagMemory 基于字符二元组的上下文检索
- [ ] 增强 `_keyword_search()` 后备搜索算法
- [ ] 实现 `verify_yaml_against_facts()`（当前是存根）
