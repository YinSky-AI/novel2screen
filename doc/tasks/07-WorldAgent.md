# 模块 07：WorldAgent（世界观构建）

> 构建小说的世界观规则（魔法、科技、政治、地理等）。仅 Long 模式运行。

## 子任务

- [x] `world.py` — AgentBase 继承，`run()` 方法实现
- [x] `world.py` — `validate()` 方法
- [x] `world.py` — `get_default_context()` Short 模式备用
- [x] 输出：`{"world_rules": {"magic": ..., "technology": ..., "politics": ...}, "geography": [...]}`
- [x] Short 模式跳过，使用默认世界观模板
