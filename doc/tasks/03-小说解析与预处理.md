# 模块 03：小说解析与预处理

> 负责小说文本的语言检测、章节解析、分块与快速预处理（Fast 模式）。

## 子任务

- [x] `preprocessor.py` — `preprocess_novel()` 一次 LLM 调用抽取叙事/角色/世界观
- [x] `preprocessor.py` — `batch_plan_episodes()` 一次 LLM 调用规划所有剧集+场景
- [x] `preprocessor.py` — JSON 解析工具函数
- [x] 演示预处理数据（固定返回）
- [ ] 合并 `novel_reader.py` — 语言检测（中/英）、章节解析（`第X章` / `Chapter X` / `Part X`）
- [ ] 合并 `novel_reader.py` — Token 估算器与智能分块
- [ ] 合并 `novel_reader.py` — 小说开头/结尾提取（fidelity 检测用）
- [ ] 快速模式预处理支持 RAG 上下文注入
