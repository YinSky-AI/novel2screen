# 模块 04：模式路由 (ModeRouter)

> 根据章节数量自动选择 Short / Long 模式，并配置 Agent 链行为差异。

## 子任务

- [x] `route_mode()` — 根据章节数返回 `"short"` 或 `"long"`
- [x] Short 模式：WorldAgent 跳过使用默认模板
- [x] Short 模式：TimelineAgent 仅线性输出，无冲突检测
- [x] Short 模式：检索 top_k = 3
- [x] Long 模式：完整 Agent 链，检索 top_k = 5
- [x] `router.py` — ModelRouter 为每个 Agent 分配模型
- [x] `router.py` — `execute()` 带指数退避的重试逻辑
- [ ] 根据设计方案 §13 更新 ModelRouter 中每个 Agent 的模型映射
