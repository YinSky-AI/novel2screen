# 模块 18：工作流编排

> 流水线编排器：Novel2ScreenWorkflow（Fast / Full 模式）

## 子任务

- [x] `workflows/novel2screen.py` — Novel2ScreenWorkflow 类
- [x] `workflows/novel2screen.py` — `fast_run()` 快速流水线（2~3 次 LLM 调用）
- [x] `workflows/novel2screen.py` — `run()` 完整流水线（9 Agent 链）
- [x] `workflows/novel2screen.py` — `_build_screenplay()` 剧本组装
- [x] `workflows/novel2screen.py` — `_init_semantic_memory()` RAG 初始化
- [x] `workflows/novel2screen.py` — `run_consistency_check()` 一致性检查
- [x] `workflows/novel2screen.py` — `save_export()` YAML 导出
- [x] `workflows/novel2screen.py` — `parse_and_segment()` 章节分割
- [ ] 修复完整流水线中 `scenes_in_episode` 键缺失问题
- [ ] 修复完整流水线中 `dialogue_idx` 索引错误
- [ ] 修复 TimelineAgent 输出未被下游使用的问题
- [ ] 合并 `orchestrator.py` 基于阶段的 PipeLine 引擎到主 `backend/`
- [ ] 统一快速/完整流水线的错误处理策略
